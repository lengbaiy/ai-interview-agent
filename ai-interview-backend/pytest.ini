[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: fast isolated tests
    integration: tests requiring application services
    smoke: application smoke tests
    slow: tests that are intentionally slower
    e2e: browser or full workflow tests
